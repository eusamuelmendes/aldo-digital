# Banco de dados (Supabase, Neon ou Postgres próprio)
DATABASE_URL="postgresql://USUARIO:SENHA@HOST:5432/aldo"

# Segredo usado para assinar os tokens dos QR Codes das mesas.
# Gere com: openssl rand -hex 32
TABLE_TOKEN_SECRET=""

# Taxa de atendimento padrão (%) — pode ser sobrescrita na tabela settings
SERVICE_FEE_PERCENT="10"

# --- AINDA NÃO CONFIGURADO (ver documento de auditoria) ---
# MERCADOPAGO_ACCESS_TOKEN=""
# MERCADOPAGO_WEBHOOK_SECRET=""
